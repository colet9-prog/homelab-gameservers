services:
  minecraft:
    image: itzg/minecraft-server
    container_name: minecraft-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      VERSION: "LATEST"
      MEMORY: "4G"
    volumes:
      - .:/data
    restart: unless-stopped
    stdin_open: true
    tty: true
